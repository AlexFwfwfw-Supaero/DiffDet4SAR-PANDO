# DiffDet4SAR Requirements
# Minimal dependencies for training on ATRNet-STAR dataset

# PyTorch (adjust CUDA version as needed)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core dependencies
numpy>=1.24.0
pillow>=10.0.0
opencv-python>=4.8.0

# Model & Training
timm>=0.9.0
tqdm>=4.65.0
pycocotools>=2.0.6
tensorboard>=2.14.0

# Detectron2 & fvcore (in-tree - no pip install needed)
# Already included in the repository

# Optional: HuggingFace for dataset download
# huggingface-hub>=0.20.0  # Only needed for download_dataset.sh
